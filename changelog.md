# Changelog

## v1.6.1 (6)
- Added guards to prevent failure on decoy obfuscated classes
- Removed buggy toast codes
- Fixed target package being replaced with default upon reboot
- Fixed WebUI components not working issue
- Fixed the interface in WebUI to support edge-to-edge

## v1.6.0 (5)
- On-screen toasts for dumper lifecycle events (load, dump start, dump end + path)
- KernelSU/APatch WebUI for switching the target package
- Update button support in Magisk/KernelSU/APatch via `updateJson`
